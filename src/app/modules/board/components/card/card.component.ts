import { Component } from '@angular/core';

@Component({
  selector: 'card',
  styleUrls: ['card.component.scss'],
  template: `
    <div>
      card
    </div>
  `
})

export class CardComponent {
  constructor() {}
}
