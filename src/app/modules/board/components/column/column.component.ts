import { Component } from '@angular/core';

@Component({
  selector: 'column',
  styleUrls: ['column.component.scss'],
  template: `
    <div>
      column
    </div>
  `
})

export class ColumnComponent {
  constructor() {}
}
